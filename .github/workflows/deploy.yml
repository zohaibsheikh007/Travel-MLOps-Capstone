name: Build and Deploy Flight Price API

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r "Flight Prediction/requirements.txt"

      - name: Train model
        run: python "Flight Prediction/train_model.py"

      - name: Run API health check
        run: |
          python -c "
          import sys
          sys.path.insert(0, 'Flight Prediction')
          from Flight_Price import app
          client = app.test_client()
          resp = client.get('/health')
          assert resp.status_code == 200
          print('Health check passed')
          "

  docker:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Train model for Docker image
        run: pip install -r "Flight Prediction/requirements.txt" && python "Flight Prediction/train_model.py"

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: "./Flight Prediction"
          push: true
          tags: |
            travelwise/flight-price-api:${{ github.sha }}
            travelwise/flight-price-api:latest

      - name: Update Kubernetes manifest
        run: |
          sed -i "s|image: .*|image: travelwise/flight-price-api:${{ github.sha }}|" "Flight Prediction/deployment.yaml"

      - name: Commit updated manifest
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git add "Flight Prediction/deployment.yaml"
          git diff --staged --quiet || git commit -m "ci: update image tag to ${{ github.sha }}"
          git push
